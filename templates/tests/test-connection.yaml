apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flux-101.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "flux-101.name" . }}
    helm.sh/chart: {{ include "flux-101.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "flux-101.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
